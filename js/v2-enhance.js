/* ============================================================
   个人主页 V2 视觉增强 · 预览专用脚本
   目前为空实现占位：V1 main.js 已覆盖全部基础交互；
   铆钉角标用纯 CSS（.card::before 多背景）实现，无需 JS。
   后续 V2 增强若需要 JS（如浮尘粒子、主题切换），写入此处。
   ============================================================ */
(function(){
  'use strict';
  // 占位：标记 V2 预览脚本已加载（便于控制台核验）
  window.__V2_PREVIEW__ = true;
})();
