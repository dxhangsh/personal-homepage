# WORKLOG · 个人主页 Vibe Coding 课程（V1 → V4）

> 本文件全程记录个人主页的开发过程，作为课程要求的过程证据（可解释、可追溯）。
> 记录原则：事实优先、可解释、结构化、渐进（一次改一类问题）。

---

## 版本链总览

| 版本 | 阶段 | 状态 | 一句话说明 |
| --- | --- | --- | --- |
| V1 | 阶段一 | ✅ 已完成 | 先做出一个能看的本地版本：胡桃木 × 橡木拟物化，含完整内容骨架 |
| V2 | 阶段二 | 🚧 进行中 | 覆盖层路线与 V1 共存：质感升级 → 柔和提亮 → 洞窟探索光效 → 引子（已完成主体，待内容补齐收尾） |
| V3 | 阶段三 | ⏳ 待开始 | 加入 Feedback 并公开发布 |
| V4 | 阶段四 | ⏳ 待开始 | 用真实反馈完成第一月版本 |

### V2 交付物与入口（与 V1 共存，不改动任何 V1 文件）

| 文件 | 作用 |
| --- | --- |
| `v2-preview.html` | V2 预览页（导航含「返回 V1 正式版」互切按钮） |
| `css/styles-v2.css` | 覆盖层样式：提亮主题 / 烫印 / 灯槽 / 铆钉 / 金线 / 引子与光效配套 |
| `js/v2-enhance.js` | 增强脚本：凿边重跑、数字递增（Number Ticker） |
| `js/cave-light.js` | 洞窟光效引擎：火把光标 / 火星粒子 / 呼吸照亮 / 探索记忆（文档坐标） |
| `js/cave-intro.js` | 洞窟引子：硫磺粉文字逐行浮现 → 木棍拿起（↑ 开启奇幻之旅）→ 粒子汇聚冲击点燃，火把与木棍位置严格重合 |
| `assets/textures/woodcut-motif.svg` | 低透明度木刻纹样层素材 |

- **入口**：`http://127.0.0.1:8124/v2-preview.html`（预览服务：`python -m http.server 8124 --bind 127.0.0.1 --directory personal-homepage`）
- **引子接口**：`window.CaveLight.begin({x,y})` 点火、`CaveLight.on('ready'|'reveal', fn)` 事件钩子
- **样式改动须递增 `?v=N` 版本参数**（`v2-preview.html` 内），否则浏览器/AdGuard 会命中旧缓存

---

## V1 · 阶段一：先做出一个能看的版本

### 目的（课程）
先让网页「存在并能被看见」，明确页面服务谁、展示什么、希望访问者做什么。

### 本轮做了什么

**1. 视觉方向确认（前置，在本项目目录外完成的视觉方向稿）**
- 反复打磨背景纹理：线条木纹 → 程序化木纹 → **AI 生成拟真实木纹**（胡桃木）
- 关键约束：本地预览服务器**无法可靠传输 >64KB 的单个文件**（实测下载在 ~65KB 处截断），因此所有被服务器提供的资产强制 <64KB
- 曾用 numpy 程序化生成木纹（23060B），后改用 AI 生成真实木纹（压缩后 32113B），真实度显著提升
- 画面语言：胡桃木背景 × 橡木卡片 × 黄铜五金，统一左上光源，卡片随机凿边（模拟切割木料）

**2. 项目结构搭建（`personal-homepage/`）**
```
personal-homepage/
├── index.html              # 单页主体（语义化标签）★ 本轮
├── css/
│   └── styles.css          # 拟物化视觉系统 ★ 本轮
├── js/
│   └── main.js             # 交互（凿边/导航/返回顶部/锚点高亮）★ 本轮
├── assets/
│   └── textures/
│       └── wood-texture.png  # 胡桃木贴图（32113B，<64KB，可无缝平铺）
└── WORKLOG.md              # 本记录
```

**3. 页面结构与内容**
- 完整信息架构：导航 → Hero → 关于我 → 项目作品 → 技能服务 → 联系我 → 返回顶部
- 语义化标签：`header/nav/main/section/article/footer`，图片预留 `alt`
- **内容槽位全部用「待补充」标记**（按用户选择「按骨架先搭」，后续逐项替换为可核验真实内容）
- 无第三方依赖，纯静态 HTML/CSS/JS

**4. 拟物化视觉实现**
- 背景：胡桃木贴图（520px/390px 平铺）+ 径向/线性光影叠加，随界面滚动
- 卡片：橡木底座 + 内凹阴影 + 去皮木纹（feTurbulence SVG）+ 顶部内缘暗落，`clip-path` 随机凿边
- 金属件：黄铜渐变 + 左上光高光 + 外阴影（按钮/图标/横幅/进度条）
- 深浅色：`:root` 深胡桃（默认）+ `[data-theme="light"]` 浅胡桃切换变量
- 响应式：桌面/平板/移动三视口用 `grid auto-fit` + 媒体查询（≤720px 导航折叠为纵向）
- 吸顶导航、返回顶部按钮、锚点平滑滚动、当前锚点高亮

### 验收结果

**1. 资源传输（服务器 <64KB 约束）**
| 资源 | 状态 | 大小 |
| --- | --- | --- |
| index.html | ✅ 200 | 7690B |
| css/styles.css | ✅ 200 | 11178B |
| js/main.js | ✅ 200 | 3489B |
| assets/textures/wood-texture.png | ✅ 200 | 32113B |

全部 <64KB，0.004~0.1s 内完整下载，无截断。

**2. 页面在真实浏览器中渲染（CDP 实测）**
- `title`：`陈胤璋 · 个人主页 V1` ✅
- 卡片数：14，全部生成随机凿边 `clip-path`（jagged 14/14）✅
- 导航链路：5 个 ✅；Hero h1 存在 ✅
- 语义化 `section[id]`：5 个 ✅
- 背景已加载 `wood-texture` 贴图 ✅；卡片内阴影生效 ✅
- `main.js` 无语法错误（node --check 通过）✅

**3. 响应式无横向溢出（CDP 模拟视口实测）**
| 视口 | innerW | scrollW | 溢出 |
| --- | --- | --- | --- |
| 桌面 1280 | 1280 | 1263 | 0 |
| 平板 768 | 768 | 751 | 0 |
| 移动 390 | 390 | 390 | 0 |

### 本地预览
- 服务器：`C:\Python314\python.exe -m http.server 8124 --bind 127.0.0.1 --directory personal-homepage`（pid 54652）
- 预览地址：**http://127.0.0.1:8124/**

### 5. 内容真实化（本轮：从个人陈述提取初步填写）
按用户选择「按骨架先搭」后，V1 骨架与视觉已定稿。现从用户上传的个人陈述图片（`uploads/ses_f9837a08cffeHcxp30aAu6vCF8/IMG_20260712_165709.jpg`）提取可核验内容，对「待补充」槽位做了**初步真实填写**：

| 区块 | 已填入（来自陈述，逐字核验） | 仍待补充 |
| --- | --- | --- |
| 身份名片 / Hero | 姓名 **陈胤璋**；定位「立志投身脑机接口前沿工科的少年」 | 头像已用 AI 木刻浮雕插画填充（V2） |
| 关于我 · 身份/方向/记忆点 | 2007 年生·广东汕头人；目标华南理工国际校区新工科（脑机接口/生物医学工程）；拆解改良旧笔记本×2/台式机/眼部按摩仪→**静音改造** | 现场/远程合作倾向确认 |
| 项目 · 01 | **拆机改良 · 眼部按摩仪静音改造**（硬件动手第一件成品） | 成果数据/照片/链接 |
| 项目 · 02 | 编程 · 机器人课程实践（方向性描述占位） | 可核验作品/成果数据 |
| 技能 · 硬件/编程/学习 | 硬件动手 + 编程基础 + 广泛阅读/运动 | 具体证书/成果 |
| 联系 / 页脚 | 姓名落款 | 关联邮箱（mailto 占位） |

**内容核验方式**：陈述图为 2048×1536，采用 System.Drawing 裁剪放大逐段核验，关键第三方信息（如「郑斯卡教授：清华生物系本科 / 约翰霍普金斯医学院博士 / 加州大学河滨分校生物医学系正教授」）单独放大到字体清晰可辨确认，避免错别字。

**关键原则**：仅填入陈述中**明确可核验**的事实；模糊/不确定内容一律保留「待补充」占位，绝不虚构。涉及真实他人的身份信息（如母亲同事教授）在未获本人确认前**不建议直接发布**，正式版需获其同意或做脱敏处理。

### 6. 复测与回归修复（内容填充后）
- 内容填充（commit `c75825c`）后复测时发现：工作区 `js/main.js` 曾被误删（未提交状态），页面脚本将 404。已用 `git restore js/main.js` 从提交恢复，工作区回到干净状态。
- 恢复后经 CDP **强制刷新（ignoreCache）实测**：`title`=「陈胤璋 · 个人主页 V1」；卡片 14/14 全部生成随机凿边 `clip-path`；导航 5；Hero h1 存在；语义化 `section[id]` 5；背景贴图已加载；**无控制台报错 / 无运行时异常**。
- 资源复测体积：index.html 7690B / styles.css 11178B / main.js 3489B / wood-texture.png 32113B，全部 HTTP 200 且 <64KB。
- 预览服务器重置于 8124：`python -m http.server 8124 --bind 127.0.0.1 --directory personal-homepage`（内容填充后旧进程已退出）。

### 第一版记录：下一轮（V2）想改进的重点
1. **内容真实化（进行中）**：✅ 头像（AI 木刻浮雕插画，V2，无水印、400×400、42661B）；✅ 隐私范围（仅保留方向性描述，去掉父母职业细节与第三方姓名）；✅ 联系邮箱（`chen_515000_bme@tju.edu.cn`，JS 防爬混淆已上线）。⬜ 待用户补充：成果数据/链接、具体就学时间线、社交账号（GitHub 等）。
2. **视觉微调候选**：凿边强度、卡片间距、木纹平铺密度是否要再调（等内容填入后再整体定稿）。
3. **深色模式**：默认深胡桃，浅胡桃切换已实现，但需在真实内容下复核对比度达标（≥4.5:1）。
4. **细节交互**：Hover 动效、表单体验（联系表单 V1 为 mailto 占位，正式表单 V3 引入）。
5. **无障碍复核**：图标按钮 aria、焦点可见性复核（当前已做基础项）。

---

## V2 · 阶段二：多轮小步打磨（进行中）

### 步骤 1：AI 插画头像替换字印（2026-09-10）
- 背景：V1 头像为「〔陈〕」木刻字印占位；用户选择「AI 生成插画头像」。
- 生成：`dth/Deepexi-Image-1.0` 出两版候选（木刻浮雕 / 动漫插画），用户选定 **A 版·木刻浮雕**。
- 处理：裁掉右下角「DeepWorks AI」水印（裁 67,0,890,890 + 缩放 400×400），导出 JPEG q80 → `assets/img/avatar-a.jpg`（42661B，<64KB）。
- 集成：`index.html` 用 `<img>` 替换字印；`styles.css` 增 `.avatar{overflow:hidden}` + `.avatar img{object-fit:cover}`，保留黄铜木牌边框。
- 验收（CDP 实测）：头像 `naturalWidth=400` 加载成功；卡片 14/14 凿边、木纹加载、无控制台报错，均保持通过。

### 步骤 2：隐私与联系方案定档
- 隐私：仅保留方向性描述（如「受母亲熏陶」），**不含**父母职业细节与第三方姓名。
- 联系：✅ 已落地 —— 邮箱 `chen_515000_bme@tju.edu.cn`，采用 JS 防爬混淆（源码以**反转分片**存储，运行时拼装；页面显示 `chen_515000_bme [at] tju.edu.cn`，按钮为 `mailto:` 并带主题）。验收：`index.html`/`main.js` 中**无明文邮箱**；按钮 `href` 与显示文本运行时拼装正确、无报错。

### 步骤 3：V2 视觉增强预览版上线（与 V1 共存）（2026-09-10）
- **依据**：`output/tasks` 交付的《v2 视觉调研》（68design.net/cool 第 1–2 页 60 站初筛 + 7 站精选：senawastudio / palmo / khanhnguyen / grounded2026 / hogwartsstory / 360lexingtonave / nbnzia）。
- **共存方式（不改动任何 V1 文件）**：
  - 新增 `v2-preview.html`（7669B）：结构与 `index.html` 同构，标题/chips/页脚标注 V2 预览，导航加「返回 V1 正式版」互切按钮，页脚加 meta 三段式（汕头 · 2026 在读 · BCI）。
  - 新增 `css/styles-v2.css`（6507B）：叠加覆盖层实现 9 项增强 —— ①烫印标题（`background-clip:text`）②卡片暖光灯槽（双层 inset 暖光）③铆钉角标（`::before` 多背景，无新增 DOM）④金线勾边（四向 `drop-shadow`，沿凿边轮廓完整生成）⑤按钮按压质感（hover 磨亮 / active 下沉+inset 阴影）⑧暗角加深（.34→.50）⑩meta 页脚 ⑪刻痕分割线（上暗下亮双线）⑫头像橡木镶框（6px 框+内阴影 vignette）。
  - 新增 `js/v2-enhance.js`（559B）：占位脚本，标记 `window.__V2_PREVIEW__`。
- **踩坑记录（重要）**：
  1. `background` **简写**会重置 `background-clip:text` 为 `border-box`，章节标题渲染成实心金块、文字不可见 —— 改用 `background-image` 修复。
  2. 金线用 `outline` 会被凿边 `clip-path` 切断（边中切深可达 3.5% 宽度）—— 改用四向 `drop-shadow`（跟随凿边实际轮廓）+ `.95` 不透明度后目检达标。
  3. hero 标题 54px 上限在 1120px 容器产生「年」孤字换行 —— clamp 上限收到 50px 修复。
- **验收（CDP 实测 `cdp_verify_v2.js`，17 项全绿）**：标题/卡片/导航/区块结构完整；`styles-v2.css` 与 `__V2_PREVIEW__` 已加载；h1 `background-clip:text` 生效；卡片暖光（255,180,90）命中；铆钉 2 颗；头像镶框 6px；meta 页脚 3 段；头像 400 加载；`mailto:` 拼装正确；互切按钮存在；无控制台报错。截图目检（首屏 + 放大）：烫印标题清晰、无孤字换行、金边连续、铆钉立体、「关于我」可读。
- **V1 回归**：`index.html` 实测不受影响（14/14 凿边、无 V2 样式引用、邮箱/头像/木纹全部正常）。
- **资源体积**：新增三文件均 <64KB（7669 / 6507 / 559B），满足服务器约束。

### 步骤 4：V2 质感升级——新区块与滚动叙事（2026-09-10）
- **依据**：多平台调研（Magic UI 组件清单 / Awwwards scrollytelling 与视差趋势 / Land-book 落地页布局 / CodePen 滚动动画 pattern / CSS 滚动驱动动画 2026 基线）。
- **新增界面区块**（内容全部可核验，不虚构）：
  1. `#milestones` 成长的刻度——4 张数字卡（拆解设备 2+ / 独立改造 1 / 出生 2007 / 方向 3），JS Number Ticker 进入视口递增（easeOutCubic，1.4s）。
  2. `#timeline` 探索的时间线——4 节点生平刻度（2007 / 童年—初中 / 高中 / 未来），左侧黄铜刻度线随滚动生长（scaleY 0→1，view() 时间线）。
  3. 兴趣词带——9 个兴趣胶囊双向无限滚动（Marquee 手法，两端渐隐遮罩，hover 暂停）。
  4. 首屏滚动提示——鼠标轮廓+下滑滑块动画（wheelDrop）。
- **滚动动效（零依赖）**：
  - `.reveal` 全站卡片进入视口浮起：CSS `animation-timeline: view()`（Blur Fade 原生版），`@supports` 包裹防旧浏览器。
  - 背景木纹双层视差（`scroll(root)` 时间线，520/390 两层不同速）。
  - `prefers-reduced-motion` 全量降级；无 `view()` 支持时内容直接可见。
- **踩坑记录（重要）**：
  1. **`.tl-dot` 被裁掉（两段式根因）**：先怀疑 `.card` 凿边 `clip-path`，移除后红点测试仍不可见；再用 CDP `elementFromPoint` + 红点高亮二分定位，确认真凶是 V1 `.card` 的 **`overflow:hidden`**——`left:-45px` 伸出卡片左缘的圆点被整颗裁掉。修复：`.tl-item.card{clip-path:none; overflow:visible}`，时间线用平直木牌（叙事上也更合理）。**教训：`getBoundingClientRect` 返回裁剪前几何值，判断"看不见"必须做像素级或 hit-test 验证。**
  2. 编辑 hero 插入滚动提示时 old_str 匹配不完整，`hero-cta` 段落被意外复制 → 目检发现重复按钮组，已删除。
  3. 时间线圆点与卡片铆钉视觉混淆 → 圆点放大至 17px 并加深色底环+金色外环，明确区分。
- **验收**：CDP 实测（`cdp_verify_v2b.js`）全绿——cssScrollAnim=true、revealUp/marqueeMove/tlGrow/woodParallax 四动画生效、词带 2 份、时间线 4 节点、数字递增至 2+/1/2007/3、无控制台报错；V1 回归无损；截图目检通过（时间线 4 节点压线确认、词带渐隐对称、滚动提示可读）。
- **资源体积**：11242 / 19513 / 1749B，均 <64KB。

### 步骤 5：柔和提亮主题（2026-09-10）
- **需求**：背景与卡片色调提亮变温和；黑金（深棕）文字统一改浅色。
- **实现**（styles-v2.css 追加第三段，仅影响预览页）：
  - 主题变量整体重定义：背景 `#3b2111→#6b4b2b` 一档提亮；橡木卡 `#c7a672→#d3b687`；暗角 `.50→.34` 减弱。
  - 深色文字全部转浅象牙：`--card-ink/sub/dim → #fff7e4/#f5ead0/#e9dab9`；`--metal-ink → #fff7e4`（按钮/chip/徽章/年份牌统一）。
  - 黄铜按钮加深半档（`#e2c584→#b3904f`）保证浅字可读。
  - 可读性辅助：全部浅色文字加双层棕色描影（text-shadow），烫印标题/大数字用 drop-shadow 衬底；kicker 提亮。
- **验收**：CDP 实测变量生效（cardInk/btnInk = rgb(255,247,228)）；截图目检——背景呈中等暖棕、卡片提亮为羊皮纸米金、无深棕黑字残留、浅字经描影后清晰可读（首屏与里程碑区块均确认）；动画与结构回归全绿（stats 终值 2+/1/2007/3）；V1 无损（14/14 凿边、无 V2 样式注入）。styles-v2.css 21838B 仍 <64KB。

### 步骤 6：洞窟探索光效引擎（2026-09-10，提交见 git log）
- **需求**：火把光标动画 + 火星粒子 + 呼吸式照亮；主界面初始全黑；光标照过区域「永久稍暗但可见」；光标周围相对最亮（洞窟探索感）；预留引入界面接口。
- **实现**（新增 `js/cave-light.js` 9995B，纯原生零依赖）：
  - **双层 canvas**：记忆层（持久累积探索痕迹）+ 黑暗层（每帧重绘盖在内容上，`pointer-events:none`）。
  - **火把光标**：DOM 火焰（外焰/内焰/焰心三层 radial-gradient + flicker 动画）+ 木柄 + 光晕；`cursor:none` 隐藏原生光标；触屏（pointer:coarse）自动回退。
  - **呼吸式照亮**：半径正弦呼吸 ±8.5% + 随机闪烁 ±5%，每帧作用于火把冲孔半径。
  - **探索记忆**：光标每移动 >6px 在记忆层盖印径向渐变；黑暗层每帧以 `destination-out + globalAlpha=memoryAlpha` 恒定强度冲孔——**路径重叠不增亮**（关键设计），实现「永久稍暗但可见」。
  - **火星粒子**：canvas 粒子（微重力+衰减+随移动加速喷发，上限 90 颗），绘制在最上层永远可见；点击爆发 14 颗。
  - **引子接口**：`window.CaveLight`——`begin(options)` 显式点火 / `enter(fn)` 与 `on('ready'|'reveal')` 事件钩子，供后续引导页控制开场节奏。默认首次 pointermove 自动点火。
  - `prefers-reduced-motion`：呼吸/闪烁/火星全停。
- **调试要点（重要经验）**：
  1. CDP 校验需先 `CaveLight.begin()` 或派发 pointermove 才启动引擎。
  2. **记忆层饱和陷阱**：`source-over`/`lighter` 累积盖印会让走过区域 alpha→1（完全可见）。最终方案：mem 画「不透明遮罩形状」，冲孔时用 `globalAlpha=memoryAlpha` 恒定控制强度——重叠不增亮，亮度语义正确。
  3. 最终参数：darkness 0.98 / memoryAlpha 0.42 / torchRadius 230 / stamp 半径 0.45R（收窄使两路径带可分辨）。
- **验收**（五点 alpha 实测：火把 45 < 路径 145/145 < 路径间 164 < 未探索 250，数值越小越亮）：三层亮度梯度肉眼可辨；火把区最亮内容清晰；两条路径带分离可辨；未探索近全黑；目检判定达到可交付水平。CDP 全套回归通过、V1 无损、cave-light.js 9995B <64KB。

### 步骤 7：洞窟光效扩展为整页坐标系（2026-09-10）
- **需求修正**：黑暗不应只覆盖首屏视口——滚到页面下方也应处于黑暗中，火把跟随光标（视口内）探索整页，痕迹对准页面位置永久留存。
- **实现**：记忆层升级为**文档坐标系全页画布**（宽=scrollWidth、高=scrollHeight）；痕迹盖印坐标改为 `clientX+scrollX / clientY+scrollY`；黑暗层保持视口大小 fixed，每帧 `drawImage(mem, -scrollX, -scrollY)` 与滚动对齐合成；`resize()` 时文档增高会**保留旧痕迹**地扩展记忆层画布。火光暖色叠加半径 0.85R→0.6R 收窄边缘。
- **验收**（CDP 五阶段滚动场景实测）：不滚动走过区 145 vs 远处 250；滚到文档深处未探索区 250 全黑（黑暗覆盖整页）；深处走过 58（深处探索正常）；滚回顶部痕迹 127 持久保留；全程无异常。V2 全套回归通过、V1 无损。

### 步骤 8：记忆层半径加大一圈（2026-09-10，提交 30a3d97）
- **需求**：火把产生的记忆痕迹半径加大一圈，贴近光照范围。
- **调参**：盖印半径 `0.45R→0.75R`（104px→173px，与光照半径 230px 更贴近）；`memoryAlpha 0.42→0.34`（面积变大后微降强度，维持「稍暗但可见」层级）。
- **验收**（像素采样）：路径中心 165、路径边缘+130px 处 169（旧半径 104px 覆盖不到该点，现已被痕迹覆盖）、未探索 250、火把 46——加宽生效且三层结构保持；目检确认渐变柔和无生硬分界。

### 步骤 9：洞窟引子（cave-intro）（2026-09-10，提交 a524e68）
- **需求**：全黑背景 + 硫磺粉材质文字逐行浮现（间隔 1.5s，7 行）；木棍「← 开启奇幻之旅」点击拿起 → 原生光标消失、木棍飞至顶部正中竖起 → 文字破碎成粒子汇聚游走数圈 → 冲向木棍穿透点燃（强调冲击力）→ 光标替换为火把、启动洞窟光效并无缝衔接主页。
- **实现**（新增 `js/cave-intro.js` 8688B，纯原生零依赖）：
  - **硫磺粉文字**：7 行中文以 `background-clip:text` 承载金黄渐变 + 双层 drop-shadow 发光；逐行 `opacity/translateY` 过渡浮现（首行延迟 0.7s、间隔 1.5s）；衬线字体栈（KaiTi/STKaiti/Noto Serif SC）。
  - **木棍与拿起**：CSS 绘制的木棍（渐变 + 内高光），hover 增亮上浮；点击后转为 `position:fixed` 并按 `translate(dx,dy) rotate(-84deg)` 飞行 0.95s 至顶部正中竖起。
  - **文字破碎**：逐行用离屏 canvas 以同款字体渲染文字 → `getImageData` 按 4px 网格采样 alpha>110 的像素生成粒子（上限 2600 保性能），原行淡出。
  - **三段粒子时序**：A 炸开（500ms，速度阻尼）→ B 以极坐标 `ang += w/60` 螺旋汇聚到木棍上方游走（1900ms，`rad` 向 `orbit` 收敛）→ C 半径 `*=0.82` 加速冲向木棍并周期性从外围补一轮（400ms），营造穿透冲击。
  - **点燃**：`CaveLight.begin({x,y})` 以木棍位置点火 + `burst()` 爆发 46 颗火星 + 径向爆闪（380ms）+ 引子容器屏震（0.32s）→ 引子淡出（0.8s）→ `CaveLight.emit('reveal')`。
  - **降级与兜底**：`prefers-reduced-motion` 跳过粒子直接点火；引子脚本自身异常时由 `window.onerror` 兜底直接 `begin()`，不会出现无光标死页。
- **配套改造（cave-light.js）**：移除「首次 pointermove 自动点火」，点火完全交由引子控制；`begin({x,y})` 支持传入初始火把坐标。
- **踩坑记录（重要）**：
  1. **`#caveIntro` 容器缺失**：引子脚本因 `document.getElementById('caveIntro')` 返回 null 而静默 `return`，表现为「引子完全不生效且无任何报错」——已在 `v2-preview.html` 补上挂载节点。
  2. **图层顺序回归**：本会话后写的「柔和提亮」`body` 覆盖把两个 radial 又排回了不透明木纹**之后**，正是另一工作线 P0-3 修掉的「死层」缺陷（暖光/暗角被木纹整块盖住）。已修正为 radial 前置 + 暗角随视口锚定（`100vw 100vh` + `no-repeat`），并在 CSS 中加注释警示后续覆盖。
- **验收**（受管浏览器实测全流程）：引子阶段 `#caveIntro` 可见、7 行文字、黑底 `rgb(6,3,1)`、`z-index:9995`、页面滚动锁定；点击木棍后 `cave-mode` 加入（光标隐藏）、木棍 `translate(0,-441px) rotate(-84deg)` 飞向顶部；4.5s 后 `CaveLight.started=true`、`#caveDark` 与 `#caveTorch` 生成、引子 `fade` 并 `hidden`、滚动解锁；截图确认火把位于视口约 12% 高度处并局部照亮主页（无引子残留、非纯黑无内容）。
- **V1 回归**：`index.html` / `styles.css` / `main.js` 零改动（`git diff` 为空），V1 页面不含任何 V2 资源引用。
- **体积**：cave-intro.js 8688B / cave-light.js 11046B / styles-v2.css 26930B / v2-preview.html 11609B，均 <64KB。

#### 步骤 9 补充：火把与木棍位置同步 + 箭头改向（2026-09-10）
- **需求**：①光标替换后火把位置必须与移上去的木棍同步；②「开启奇幻之旅」箭头改为向上指向木棍。
- **根因与修复**：
  1. **火把从未被设置初始位置**：`cave-light.js` 的 `start(x,y)` 虽接收坐标并写入 `engine.mx/my`（影响光照冲孔），但**没有设置火把 DOM 的 `transform`**——元素停留在默认位置（左上角），与飞到顶部的木棍严重错位。修复：`start()` 内建元素后立即 `torch.style.transform = translate(mx,my)`。
  2. **木棍落点偏移 9px**：木棍带基础旋转 `rotate(-6deg)`，`getBoundingClientRect()` 返回的是**旋转后外接框**（高度被放大到 ~31px 而非实际 13px），用它换算落点会累积偏差；且原公式 `dy = (tip.y + r.height/2) - (r.top + r.height/2)` 多加了一个半高。修复：利用「旋转不改变元素中心」的性质——先取视觉中心 `(cx,cy)`，再用**未旋转尺寸** `offsetWidth/offsetHeight` 反推 `left/top`，最后以 `dx = tip.x - cx`、`dy = tip.y - cy` 做中心对齐。实测落点偏差归零。
  3. **箭头改向**：`← 开启奇幻之旅` → `↑ 开启奇幻之旅`。
  4. **消除"两根棍子"重影**：点燃瞬间把引子木棍 `opacity` 淡出（0.35s），与火把出现交叉过渡。
- **验收**（受管浏览器实测）：木棍最终中心 `(640,86)` 与目标点偏差 `[0,0]`；火把 `translate(640px,86px)`、`getBoundingClientRect` 同为 `(640,86)`——三者严格重合；截图目检确认**仅一支火把、无重影、无引子残留**、火把位于视口水平正中上方并照亮周围。
- **V1 回归**：`index.html` / `styles.css` / `main.js` 零改动。
- **体积**：cave-intro.js 9405B / cave-light.js 11240B，均 <64KB。

#### 步骤 9 补充二：火把与光照中心平滑交接（2026-09-10）
- **现象**：点燃后火把正确出现在顶部（木棍处），但**只要移动光标，火把与光照就瞬间跳回光标位置**，观感像「掉回下面」。
- **根因**：`onMove()` 直接把光照位置写成鼠标坐标（`this.mx = e.clientX`），且每帧绘制与 DOM transform 都引用该值——没有任何过渡，故首次移动必然瞬移。
- **修复（目标点 + 缓动追随模型）**：
  1. 新增 `tx/ty`（光标目标）与 `mx/my`（火把实际渲染位置）双坐标；`onMove()` **只更新目标**，不再直接改 `transform` 与光照位置。
  2. `loop()` 每帧按 `mx += (tx-mx) * k` 缓动追随，并同步 DOM `transform`，光照冲孔、火星喷发、记忆盖印全部改用缓动后的实际位置（视觉与逻辑一致）。
  3. **交接滑行**：点燃时 `mx/my = tx/ty = 木棍位置`（顶部），并置 `handoff=true`；自用户首次移动起计时 **700ms** 内用较慢系数（0.06）滑行，到期切回常规跟手系数（0.3）。
  4. 交接期不盖印、不喷火星，保持「火把从木棍处飘来」的观感。
- **踩坑记录**：
  - **首帧误判交接完成**：最初用「距离 < 2px 则结束交接」判定，但点燃时 `tx == mx`（距离为 0），标志在首帧即被清除，滑行完全失效（实测仍是瞬移）。修法：新增 `moved` 标志，只有用户真正移动过光标后才开始计时。
  - **指数缓动渐近不收敛**：改用定时窗口（700ms）收尾，而非距离阈值——指数缓动理论上永不归零，距离阈值会永远不满足。
- **验收**（受管浏览器实测）：
  - 点燃后未移动：火把与光照中心均为 `(640,86)`，`handoff=true`。
  - 光标移至 `(640,560)`：位置采样 `86 → 233 → 348 → 414 → 459 → 490 → 515 → 543 → 558 → 560`（约 900ms 渐进滑行，**非瞬移**）。
  - 交接结束后跟手性：目标 `(300,200)` → 实测 `(302,202)`，`handoff=false`。
  - 截图目检：单支火把、无重影；画面中另一处暖色光斑为**探索记忆痕迹**（火把滑行路径留下的永久痕迹），属设计内效果。
- **V1 回归**：`index.html` / `styles.css` / `main.js` 零改动。
- **体积**：cave-light.js 13161B，<64KB。

#### 步骤 9 补充三：删除占位卡片 + OJO 设计审计优化（2026-09-10）
- **删除占位内容**（`v2-preview.html`）：
  - 删除 2 张纯占位卡：「数字分身入口（原型）」（静态占位说明）、「FEATURED · 03」（全字段待补充）。
  - 清理 3 处 `【待补充】` 标记：「所在城市」卡的待确认问句、项目 02 的成果 chip、页脚占位说明。
  - 项目 02 文案改写为可核验的方向性描述，去掉「暂以占位」字样。
  - 结果：卡片 14 → **11 张**，`待补充` 归零。
- **OJO 设计审计（app-ui-ux-best-practices）**——量化审计发现 4 项问题：
  | 级别 | 问题 | 实测 |
  | --- | --- | --- |
  | Critical | 卡片浅字对比度严重不足 | 标题 1.82:1 / 正文 1.41:1（需 ≥4.5） |
  | Critical | 深底弱化文字不达标 | `#cbb086` = 2.86:1 |
  | Critical | 黄铜按钮象牙字不达标 | `#fff7e4` on `#b3904f` = 2.80:1 |
  | High | Muted Theme Convergence | 背景/卡片/按钮全在同一暖棕色族，无对冲色 |
- **根因与解法（关键设计决策）**：卡片浅字不达标的根因是「卡片提亮为浅米色」与「文字改浅色」**物理冲突**——浅底浅字不可能达到 4.5:1。解法：卡片改为**内凹雕刻木牌**（`#5d3f20`，比背景 `#7f5d3a` 更暗）：
  - 物理真实：V1 卡片本就用 inset 内凹阴影，雕刻凹槽理应比周围更暗；
  - 浅字在暗底上自然达标（标题 8.95 / 次级 7.99 / 正文 6.91:1）；
  - 边界不靠明度差，改由**金线勾边 + 铆钉 + 外投影**建立。
  - 同时：深底弱化文字提亮至 `#f0e0c4`（4.58:1）、黄铜压深为古铜 `#8f6c33`（4.51:1）。
- **对冲色（破解单一棕色族）**：引入**铜锈绿** `#a8cbb4`（氧化黄铜的真实材质伙伴）作小面积语义强调——章节标题竖条、词带首标签、时间线节点环、焦点环。3.36:1 ≥ UI 3:1 要求。
- **二次目检修复**：
  1. 外投影被 `clip-path` 裁掉（V1 凿边会裁切自身阴影）→ 改用父级 `.grid` 的 `drop-shadow` 垫影子层，绕过 clip 体系。
  2. 「关于我」5 卡在 3 列网格下末行孤卡留白 → 末张 `grid-column:1/-1` 跨列填满。
  3. 能力光谱进度条与卡片底接近 → 轨道压深、条身提亮。
  4. 返回顶部按钮暗底不显眼 → 尺寸 46→50px、加金线外环。
- **最终验收**（7 项对比度全部达标）：卡片标题 8.95 / 次级 7.99 / 正文 6.91 / 深底正文 5.08 / 深底弱化 4.58 / 按钮字 4.51 / 青绿 3.36:1。截图目检：卡片浮起感明显、文字清晰、青绿点缀可见、结构完整（11 卡 / 7 sections）。
- **遗留说明**：跨列卡与同排卡边缘存在细微不齐，源于 V1 随机凿边（±5% 不规则木刻边），属设计特征而非排版缺陷。
- **V1 回归**：`index.html` / `styles.css` / `main.js` 零改动。
- **体积**：styles-v2.css 34558B / v2-preview.html 10819B，均 <64KB。

---

### 步骤 10：同类调性参考站检索与 V2 优化建议（2026-09-10，并行工作线）

- **目标**：在 68design「超级酷站」中找出与当前「胡桃木 × 橡木 × 黄铜」拟物化调性相近的网站，反推出 V2 收尾阶段的优化清单。
- **检索约束（实测）**：该栏目**无站内搜索框**，仅有「行业分类 `?cate=`」与「颜色筛选 `?color=`」；颜色仅 8 个色系（红/橙/绿/蓝/紫/白/灰/黑），**无棕色系**，因此无法直接按色检索。全站共 221 页 / 约 6612 条。
- **取样策略**：橙色系 `?color=02` 全 12 页（337 条）+「家居建材 / 饮食行业 / 文化艺术 / 摄影图片」四个高相关行业各前 4 页，合并去重得 **800 个候选**；缩略图全部下载成功（fail=0）。
- **筛选方法**：PowerShell + System.Drawing 逐像素统计（暖色判定 R>G>B 且 R−B>22，暗部 lum<70），评分 = 50×暖色占比 + 20×明度贴合（目标 0.35）+ 15×(1−色相偏离（目标 28°）) + 10×暗部占比；取前 36 名拼版人工判读，最终选定 10 个调性最接近的样例。
- **10 个样例**：laesk.dk、enotecadellavalpolicella.it、punkt-gdynia.pl、code-furniture.com、firstsipbrewbox.com、sageeast.com、shoprooof.com、zecchinon.com、maplefromquebec.ca、cartapani.it。
- **交付物**：`outputs/wood-style-ref/index.html`（预览页 + `assets/` 12 张压缩图，均 <64KB）。

#### 本轮实测发现的现有缺陷

| 项 | 实测结果 | 影响 |
| --- | --- | --- |
| Hero 标题孤字换行 | `.hero-line`（54px）在 1280px 视口下渲染为 3 行，行宽 **265 / 848 / 53px**，末行仅一个「年」字 | 版面精致度受损，属可直接修复的缺陷 |

#### V2 优化建议（共 13 条，已按优先级分级）

- **P0 立即（1–5）**：①标题改用衬线字体建立工艺气质 ②橡木卡片降饱和、去「干橙」③强化 Hero 顶部暖光聚焦 ④凿边幅度 ±3.5% → ±5% ⑤修复 Hero 标题孤字换行
- **P1 建议（6–9）**：⑥放大留白（grid 20→26px / 卡片内距 26→30px / 章节距 38→52px）⑦chip 改「金属铭牌」⑧卡片补外部落影 ⑨增加低透明度木刻纹样层
- **P2 进阶（10–13）**：⑩真实照片「木框相片」⑪章节编号与时间刻度 ⑫黄铜刻度滚动进度条 ⑬无障碍与浅色模式收尾

#### 核对与验收

- **建议引用的数值均已逐一核对属实**：`--oak-base:#c7a672`、`--metal-a:#f4dfa0`、`radial-gradient(1200px 760px at 12% 4%, rgba(255,224,176,.14), ...)`、`.card{padding:26px}`、`.grid{gap:20px}`、`.section-title{margin:38px 0 16px}`、`jaggedRects()` 扰动 `amt=3.5`；且全站仅一处 `font-family`（正文无衬线，标题未单独覆盖），佐证「标题需衬线化」的判断。
- **验收（CDP 实测）**：预览页 12 张图片全部加载成功（`naturalWidth>0`）、10 个样例卡片与 13 条建议渲染正常、无脚本异常。
- **交付预览**：`http://127.0.0.1:8125/`（`python .deepworks/tmp/serve.py 8125 outputs/wood-style-ref`）。
- **过程说明（避免踩坑复现）**：本地预览服务需保证同一端口仅一个进程；Windows 下 `SO_REUSEADDR` 允许重复绑定，曾出现两进程争抢同一端口导致浏览器随机丢图，已通过「先查占用 → 清理 → 单进程重启」解决，并在页面内加入图片失败自动退避重试的自愈脚本。

---

### 步骤 11：P0 打磨（建议 1–5）合并进覆盖层（2026-09-10，并行工作线）

- **背景（重要）**：V2 打磨一度出现**两条并行实现** ——
  - **路线 A**：`v2-preview.html` + `css/styles-v2.css` + `js/v2-enhance.js`（覆盖层，**不改任何 V1 文件**，已落地建议 1/2/3/4/5/8/10/11/12 共 9 项）。
  - **路线 B**：独立 git worktree `personal-homepage-polish`（分支 `v2-polish`）**直接改写 V1 的 `styles.css` / `main.js`**，落地 P0-1 衬线 / P0-2 降饱和 / P0-3 图层序 / P0-4 凿边。
  - **决策：合并进路线 A** —— 把 B 的 P0 成果全部改写成**纯覆盖层**实现（`styles-v2.css` 走 `:root` 覆盖与 `body` 重写、`v2-enhance.js` 重跑凿边算法），V1 文件保持零改动；分支 B 已打 tag **`v2-polish-archive`** 留档后弃用。
- **① 修掉一个「写了但完全不渲染」的死层（P0-3）**：路线 A 与 V1 都把**不透明**木纹（`--wood`，300×300、alpha 全 255）排在两个 radial **之前**；CSS 中靠前图层在最上方，故暖光与暗角被木纹整块盖住。**A 的首版只把 alpha 由 `.14/.34` 调到 `.16/.5`，同样无效** —— 它并没有修复遮挡。修法：把两个 radial 提到木纹之前，并按新顺序重排 `background-size`（`auto, auto, 520px 520px, 390px 390px, auto`）；暖光由 `900px 560px at 15% 3%` 放大为 `1700px 1500px at 12% 0%`。
- **② 橡木色板降饱和（P0-2）**：走 `:root` 覆盖。**明度不变**、饱和度 S≈43%→33%（深色档 `--oak-base:#c7a672`→`#bda77c`，浅色档 `--oak-base:#d2b17c`→`#c8b286`，`hi/lo` 同步），解决「干橙」感。
- **③ 衬线 × 黄铜烫印合并（P0-1 × 建议 1）**：新增 `--serif` 字体栈（Georgia / Songti SC / Noto Serif SC / SimSun / Times New Roman）承载原有的黄铜渐变填充（`background-clip:text`），`.hero-line`、`.section-title h2`、`.card h3` 统一衬线。
- **④ Hero 孤字（建议 5）按衬线字宽重定字号**：衬线字面比无衬线宽，**沿用 A 的 50px 会再次断行**。实测（1280 视口）：`hero-body` 可用宽 874px，第二行 17 字 ×（字号 − 1px 字距）→ **52px 仍为单行、53px 起断行**。故桌面档（`@media ≥1164px`，此时 `.wrap` 已封顶 1120px、可用宽恒定）取 **52px**；窄屏退回 `4vw`；并加 `text-wrap:balance` 兜底移动端孤字。
- **⑤ 凿边幅度 ±3.5% → ±5%（P0-4）**：V1 的 `jaggedRects` 是 IIFE 内部函数、**未导出**，无法直接调参；但其唯一副作用是写 `el.style.clipPath`。因 `v2-enhance.js` 加载在 `main.js` **之后**，按同一算法（每边插点 4/5/4/5 = 18 点）重跑一遍即可调参，**完全不触碰 V1 的 `main.js`**。

#### 本轮实测（CDP 实测 + 像素对账）

- **图层遮挡解除**：V2 相对 V1，左缘木纹（x=30）感知亮度增量随深度单调衰减 —— `y120 +35.5 / y300 +27.5 / y560 +14.4 / y880 +4.0`；右上 `1250,180` 仅 `+2`（仅暖光余量，符合椭圆模型）。
- **色板**：`.card` 计算值 `rgb(189,167,124)` = `#bda77c` ✅
- **标题**：`font-family` 命中衬线栈、`font-size:52px`、`text-wrap:balance`；第二行 `getClientRects().length = 1`（**无孤字**），行右缘 1121px ≤ 容器 1136px（余量 15px）。
- **凿边**：14 张卡片 × 18 点，最大法向偏移 **4.32–4.99%**（`amt=5`，未越界）。
- **页面**：14 卡片、页脚与 meta 三段式齐全、邮箱运行时还原 `chen_515000_bme [at] tju.edu.cn`、无脚本报错；页高 2266 → **2271**（衬线字面差异，+5px）。
- **体积**：`styles-v2.css` 6507 → **7932B**、`v2-enhance.js` 559 → **1974B**，仍远小于 64KB。
- **V1 回归**：`git diff --stat` 仅有 `styles-v2.css`、`v2-enhance.js` 两个文件；`index.html` / `styles.css` / `main.js` **零改动**。

#### 新发现的待决项（本轮未改，留待确认）

- **「暗角加深」（建议 8）位置失效**：两个 radial 用 `background-size:auto`，其定位区等于 **body 全高（2271px）**，故 `at 92% 102%` 的圆心落在 `y≈2316`（页面底部），**前 ~1800px 完全够不到** —— 实测右下 `1250,880` 的 V2/V1 亮度差为 **0**。若要做成真正的页面暗角，需把这两个 radial 改为随视口锚定（`background-attachment:fixed`）或改挂到固定定位伪元素。**此项会改变路线 A 的既有观感，故先注明、不动手。**

---

*记录时间：2026-09-04 起始 · 2026-09-10 内容填充与复测、V2 头像与联系邮箱落地、参考站检索与优化建议成稿、**P0 打磨（建议 1–5）合并进覆盖层并修复图层遮挡死层** · 阶段：项目启动 → V1 完成 → 复测通过 → V2 打磨中*

---

## 工作线说明（避免步骤编号混淆）

V2 期间曾有两条并行推进的工作线，本节起按时间归并编号：

- **主线（本文件步骤 1–8、11）**：`v2-preview.html` + `css/styles-v2.css` + `js/v2-enhance.js` + `js/cave-light.js` + `js/cave-intro.js` 的**覆盖层路线**，不改动任何 V1 文件。
- **并行线（步骤 9–10）**：参考站检索与 P0 打磨（衬线化、降饱和、图层序修复、凿边幅度、暗角锚定），其成果已**全部改写为覆盖层实现**并入主线；对应的独立 worktree 分支已打 tag `v2-polish-archive` 留档弃用。
- **注意**：两线都曾写过 `body` / `:root` 覆盖，**后写者生效**。修改 `styles-v2.css` 时须检查是否与既有覆盖冲突（尤其 `body` 的 `background-image` 图层顺序，见步骤 11 踩坑 2）。

---
